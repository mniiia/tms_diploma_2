const baseEndpoint:string = 'https://api.itbook.store/1.0'

const newBooksEndpoint:string = '/new'
const bookEndpoint:string = '/books/'

export {
  baseEndpoint,
  newBooksEndpoint,
  bookEndpoint
}
